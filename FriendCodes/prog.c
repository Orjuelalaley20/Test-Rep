#include <stdio.h>
#include <stdlib.h>

int main()
{
    char palabra[20];
    int n;
    scanf("%s",palabra);
    scanf("%d",&n);
    system("clear");
    for (int i = 0; i < ("%d",n) ; i++)
    {
        printf("%s\n", palabra);
    }
    printf("\n");
    return 0;
}